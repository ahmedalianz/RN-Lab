export const SPLASH_BOOT_LINES = [
  'Hermes runtime online',
  'Fabric renderer attached',
  'Workbench modules loaded',
] as const;

export const SPLASH_TIMING = {
  hideNativeMs: 120,
  logoMs: 520,
  titleDelayMs: 180,
  titleMs: 420,
  lineStaggerMs: 140,
  lineMs: 280,
  holdMs: 420,
  exitMs: 380,
} as const;
